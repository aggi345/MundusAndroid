<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="is.hi.HBV601G.mundusandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.ChildActivities.MarketplaceChildActivity"></activity>
        <activity android:name=".Activities.ParentActivities.MarketplaceActivity" />
        <activity android:name=".Activities.ChildActivities.QuestLogChildActivity" />
        <activity android:name=".Activities.ParentActivities.QuestLogParentActivity" />
        <activity android:name=".Activities.ChildActivities.MainMenuChildActivity" />
        <activity android:name=".Activities.ParentActivities.CreateRewardParentActivity" />
        <activity android:name=".Activities.ParentActivities.createQuestParentActivity" />
        <activity android:name=".Activities.ParentActivities.MainMenuParent" />
        <activity android:name=".Activities.SignupActivity" />
        <activity android:name=".Activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>